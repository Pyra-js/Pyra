import { defineConfig } from '@pyra/shared';

/**
 * Minimal Pyra Configuration
 *
 * This is a zero-config setup. Pyra will use sensible defaults:
 * - Entry: src/index.ts
 * - Output: dist/
 * - Dev server: http://localhost:3000
 * - HMR enabled
 */
export default defineConfig({});
